---
import { type CollectionEntry, getCollection, render } from 'astro:content';
import Layout from '../../../layouts/Layout.astro';
import Footer from '../../../components/Footer.astro';
import Badge from '../../../components/ui/Badge.astro';
import { languages, useTranslations, type Lang } from '../../../i18n/index';
import { getLocalizedPath } from '../../../i18n/utils';

export async function getStaticPaths() {
    const publications = await getCollection('publications');
    const paths = [];

    for (const lang of Object.keys(languages)) {
        for (const publication of publications) {
            // Remove .md extension from slug
            const slug = publication.id.replace(/\.md$/, '');
            paths.push({
                params: { lang, slug },
                props: { publication, lang },
            });
        }
    }

    return paths;
}

interface Props {
    publication: CollectionEntry<'publications'>;
    lang: Lang;
}

const { publication, lang } = Astro.props;
const t = useTranslations(lang);
const { Content } = await render(publication);
---

<Layout title={`${publication.data.title} - Jonas Wilinski`} lang={lang}>
    <main class="pt-24 pb-16">
        <!-- Back Button -->
        <div class="px-6 md:px-8 mb-8">
            <div class="max-w-3xl mx-auto">
                <a
                    href={getLocalizedPath('/publications', lang)}
                    class="inline-flex items-center gap-2 text-sm text-neutral-500 dark:text-neutral-400 hover:text-accent transition-colors"
                >
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
                    </svg>
                    {t('publications.backToPublications')}
                </a>
            </div>
        </div>

        <!-- Header -->
        <header class="px-6 md:px-8 mb-12">
            <div class="max-w-3xl mx-auto text-center">
                <!-- Meta -->
                <div class="flex flex-wrap items-center justify-center gap-4 text-sm text-neutral-500 dark:text-neutral-400 mb-6" data-reveal>
                    <span class="font-mono">{publication.data.year}</span>
                    <span class="text-neutral-300 dark:text-neutral-700">•</span>
                    <Badge variant="accent" size="sm">
                        {publication.data.venue}
                    </Badge>
                </div>

                <!-- Title -->
                <h1 class="font-display font-bold text-3xl md:text-4xl lg:text-5xl text-neutral-900 dark:text-white mb-6 leading-tight" data-reveal>
                    {publication.data.title}
                </h1>

                <!-- Authors -->
                <div class="text-lg text-neutral-600 dark:text-neutral-400 mb-8" data-reveal>
                    {publication.data.authors.join(', ')}
                </div>

                <!-- Actions -->
                <div class="flex flex-wrap items-center justify-center gap-4" data-reveal>
                    {publication.data.pdf && (
                        <a
                            href={publication.data.pdf}
                            target="_blank"
                            rel="noopener noreferrer"
                            class="inline-flex items-center gap-2 px-6 py-3 bg-neutral-900 dark:bg-white text-white dark:text-neutral-900 rounded-full font-medium hover:bg-accent dark:hover:bg-accent hover:text-white dark:hover:text-white transition-all shadow-lg hover:shadow-accent/25"
                        >
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                            </svg>
                            {t('publications.downloadPdf')}
                        </a>
                    )}
                    {publication.data.link && (
                        <a
                            href={publication.data.link}
                            target="_blank"
                            rel="noopener noreferrer"
                            class="inline-flex items-center gap-2 px-6 py-3 bg-white dark:bg-neutral-800 text-neutral-700 dark:text-neutral-300 border border-neutral-200 dark:border-neutral-700 rounded-full font-medium hover:border-accent hover:text-accent dark:hover:border-accent dark:hover:text-accent transition-all"
                        >
                            {publication.data.linkText || t('publications.viewOriginal')}
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                            </svg>
                        </a>
                    )}
                </div>
            </div>
        </header>

        <!-- TL;DR & Key Points -->
        {(publication.data.tldr || (publication.data.keyPoints && publication.data.keyPoints.length > 0)) && (
            <section class="px-6 md:px-8 mb-12" data-reveal>
                <div class="max-w-3xl mx-auto bg-neutral-50 dark:bg-neutral-900/50 rounded-2xl p-6 md:p-8 border border-neutral-100 dark:border-neutral-800">
                    {publication.data.tldr && (
                        <div class="mb-8 last:mb-0">
                            <h2 class="font-display font-bold text-lg text-neutral-900 dark:text-white mb-3 flex items-center gap-2">
                                <span class="text-accent">TL;DR</span>
                            </h2>
                            <p class="text-neutral-700 dark:text-neutral-300 leading-relaxed">
                                {publication.data.tldr}
                            </p>
                        </div>
                    )}

                    {publication.data.keyPoints && publication.data.keyPoints.length > 0 && (
                        <div class="last:mb-0">
                            <h2 class="font-display font-bold text-lg text-neutral-900 dark:text-white mb-4">
                                {t('publications.keyPoints')}
                            </h2>
                            <ul class="space-y-3">
                                {publication.data.keyPoints.map(point => (
                                    <li class="flex items-start gap-3 text-neutral-700 dark:text-neutral-300">
                                        <svg class="w-5 h-5 text-accent shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                                        </svg>
                                        <span>{point}</span>
                                    </li>
                                ))}
                            </ul>
                        </div>
                    )}
                </div>
            </section>
        )}

        <!-- Abstract / Content -->
        <article class="px-6 md:px-8" data-reveal>
            <div class="max-w-3xl mx-auto">
                <h2 class="font-display font-bold text-2xl text-neutral-900 dark:text-white mb-6">
                    {t('publications.abstract')}
                </h2>
                <div class="prose prose-neutral dark:prose-invert prose-lg max-w-none
                    prose-p:text-neutral-700 dark:prose-p:text-neutral-300 prose-p:leading-relaxed
                    prose-a:text-accent prose-a:no-underline hover:prose-a:underline
                ">
                    <Content />
                </div>
            </div>
        </article>
    </main>

    <Footer lang={lang} />
</Layout>
